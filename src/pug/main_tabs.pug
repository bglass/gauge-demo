mixin button(target)
  button.tablinks(id= "btn"+target 
    onclick='tab_select(event, "'+target+'")')= target

mixin item(id)
  .square
    .content
      .table
        .gauge(id=id)


.tab
  +button("Basic")
  +button("Clocks")
  
#Basic.tabcontent
  .grid3
    +item("T1")
    +item("T2")
    .square
      .content
        include input.pug
    +item("T3")
    +item("T4")

#Clocks.tabcontent
  #C1
